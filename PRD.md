# PRD — 4시간 MVP 개인 생산성 향상 에이전트

> **문서 상태:** 구현용 확정안  
> **가칭:** Focus Agent  
> **개발 시간:** 4시간  
> **제품 형태:** 로그인 없는 반응형 웹 앱  
> **핵심 기술:** GitHub Copilot SDK, Microsoft Agent Framework, Microsoft Azure

---

## 1. 제품 개요

Focus Agent는 개인이 해야 할 목표를 입력하면 AI가 실행 가능한 단계로 나누고, 단계별 결과물 작성과 완료 검토를 도와주는 개인 생산성 에이전트 앱이다.

사용자는 로그인이나 복잡한 설정 없이 목표와 완료 조건을 입력한다. 에이전트는 `계획 → 실행 지원 → 검토 → 회고` 흐름을 제공하며, 진행 상태와 결과를 한 화면에 보여준다.

### 한 줄 가치 제안

> 해야 할 일을 한 문장으로 입력하면, AI가 지금 실행할 계획과 결과물을 만들고 완료 여부까지 확인해준다.

---

## 2. 문제 정의

개인 사용자는 해야 할 목표가 있어도 다음 문제로 실행에 어려움을 겪는다.

- 목표가 크거나 모호해 첫 행동을 정하기 어렵다.
- 계획 작성과 실제 결과물 작성이 분리되어 중간에 맥락이 유실된다.
- 진행 중 무엇을 끝냈고 무엇이 남았는지 파악하기 어렵다.
- 완료했다고 느끼더라도 처음 세운 조건을 충족했는지 확인하기 어렵다.

Focus Agent는 목표, 실행 계획, AI 결과물, 완료 검토를 하나의 작업 세션에 묶어 개인이 더 빠르게 결과에 도달하도록 돕는다.

---

## 3. 목표 사용자

### 주 사용자

- 해야 할 일을 빠르게 구조화하고 싶은 개인
- 발표, 문서, 학습, 개발 등 지식 작업을 수행하는 개인 사용자
- 여러 생산성 도구를 설정하지 않고 즉시 AI 도움을 받고 싶은 사용자

### 핵심 Job-to-be-Done

> “내 목표를 입력하면 지금 할 수 있는 단계로 정리하고, 필요한 초안을 만든 뒤 완료 조건까지 확인하고 싶다.”

### 사용 단위

- 사용자: 개인 1명
- 세션: 목표 1개
- 동시 실행: 세션당 1개
- 협업, 조직, 역할 권한은 지원하지 않는다.

---

## 4. MVP 목표

4시간 내 아래 사용자 흐름을 실제 배포 환경에서 시연한다.

1. 사용자가 목표와 완료 조건을 입력한다.
2. AI가 3~5개의 실행 단계와 예상 결과물을 제안한다.
3. 사용자가 계획을 승인한다.
4. AI가 각 단계에 필요한 초안, 체크리스트 또는 요약을 생성한다.
5. 사용자가 단계를 완료 처리하거나 수정 요청한다.
6. AI가 완료 조건별 충족 여부와 다음 행동을 제시한다.
7. 결과와 회고를 Markdown으로 복사하거나 내려받는다.

### MVP 성공 정의

- 새 사용자가 설명 없이 2분 안에 첫 계획을 생성한다.
- 하나의 데모 목표가 계획 생성부터 완료 검토까지 중단 없이 실행된다.
- 새로고침 후 현재 목표와 생성 결과가 유지된다.
- 모바일 360px 화면에서 핵심 흐름을 사용할 수 있다.

---

## 5. 핵심 사용자 여정

### 5.1 목표 생성

사용자는 첫 화면에서 다음 정보를 입력한다.

- 목표: 필수, 최대 500자
- 완료 조건: 필수, 최대 5개
- 참고 내용: 선택, 최대 2,000자

예시 목표:

> “내일 5분 해커톤 발표를 준비하고 발표 대본과 점검표를 완성하고 싶어.”

### 5.2 AI 계획

AI는 다음 구조로 계획을 생성한다.

- 목표 요약
- 3~5개의 실행 단계
- 단계별 예상 결과물
- 완료 조건과 단계의 연결

사용자는 계획을 승인하거나 한 번의 수정 요청을 할 수 있다.

### 5.3 실행 지원

승인 후 AI는 순서대로 단계별 결과물을 생성한다.

- 문서 초안
- 체크리스트
- 핵심 요약
- 다음 행동

사용자는 각 단계를 `완료`, `수정 요청`, `건너뛰기`로 처리한다.

### 5.4 완료 검토

모든 단계가 종료되면 AI가 완료 조건을 검토한다.

- `충족`: 생성 결과에서 근거를 찾을 수 있음
- `미충족`: 필요한 결과가 없음
- `확인 필요`: 사용자의 실제 행동을 AI가 검증할 수 없음

AI는 미충족 항목과 가장 중요한 다음 행동을 제안한다.

### 5.5 결과 저장

사용자는 목표, 계획, 결과물, 검토표를 하나의 Markdown 문서로 복사하거나 내려받을 수 있다.

---

## 6. 기능 요구사항

### P0 — 반드시 구현

| 기능 | 요구사항 | 완료 조건 |
|---|---|---|
| 목표 입력 | 목표, 완료 조건, 참고 내용 입력 | 유효성 검증 후 세션 생성 |
| AI 계획 생성 | 목표를 3~5단계로 분해 | 구조화된 계획 카드 표시 |
| 계획 승인 | 승인 또는 수정 요청 | 승인 후 실행 단계로 전환 |
| 단계별 실행 | 단계별 텍스트 결과물 생성 | 진행 상태와 결과 스트리밍 |
| 진행 관리 | 완료, 수정 요청, 건너뛰기 | 현재 단계와 전체 진행률 표시 |
| 완료 검토 | 조건별 충족 상태와 근거 생성 | 충족·미충족·확인 필요 표시 |
| 로컬 복구 | 브라우저에 현재 세션 저장 | 새로고침 후 동일 상태 복원 |
| 결과 내보내기 | 전체 결과를 Markdown으로 생성 | 복사 또는 파일 내려받기 |
| 반응형 UI | 데스크톱과 모바일 지원 | 360px 이상에서 전체 흐름 사용 |
| 오류 처리 | API 오류와 재시도 제공 | 실패 이유와 재시도 버튼 표시 |

### 시간이 남으면 구현

- 예시 목표 3개
- 단계별 소요 시간 표시
- 전체 세션 초기화
- 다크 모드

### 명시적 비목표

- 회원가입 및 로그인
- 여러 사용자 또는 실시간 협업
- 프로젝트와 칸반 보드
- GitHub 저장소, 브랜치, PR 연동
- 파일 업로드와 외부 서비스 연동
- 예약 실행과 백그라운드 작업
- 사용자 정의 에이전트
- PostgreSQL, Redis, Service Bus
- 결제, 조직, RBAC, 장기 저장
- 실제 이메일 발송, 파일 변경 등 외부 쓰기 작업

---

## 7. 화면 구조

MVP는 하나의 반응형 페이지로 구성한다.

### 7.1 시작 화면

- 제품 설명 한 줄
- 목표 입력
- 완료 조건 입력
- 참고 내용 입력
- `계획 만들기` 버튼

### 7.2 작업 화면

- 상단: 목표와 전체 진행률
- 중앙: 현재 단계, AI 스트림, 생성 결과물
- 하단: 완료, 수정 요청, 건너뛰기 버튼
- 보조 영역: 전체 단계 목록과 상태

### 7.3 완료 화면

- 완료 조건 검토표
- 생성된 결과물 모음
- 가장 중요한 다음 행동
- Markdown 복사·내려받기
- 새 목표 시작

---

## 8. 에이전트 설계

복잡한 멀티 에이전트 병렬 실행 대신 하나의 순차 워크플로를 사용한다. 사용자에게는 역할별 진행 단계를 명확하게 표시한다.

### 역할

- `Planner`: 목표를 3~5개의 실행 단계로 변환
- `Coach`: 각 단계에 필요한 결과물과 다음 행동 생성
- `Reviewer`: 완료 조건별 상태와 근거 검토

### 실행 순서

```text
GoalInput
  → Planning
  → WaitingForPlanApproval
  → ExecutingStep
  → Reviewing
  → Done
```

오류 발생 시 `Failed`로 전환하고 사용자가 현재 단계부터 한 번 재시도할 수 있다.

### 구조화 출력

AI 응답은 자유 텍스트가 아닌 JSON 스키마로 검증한다.

- 계획: `goalSummary`, `steps[]`, `expectedArtifact`, `criteriaIds[]`
- 단계 결과: `summary`, `artifact`, `nextAction`
- 검토: `criterion`, `status`, `evidence`, `nextAction`

### 실행 제한

- 계획 단계: 최대 5개
- 계획 수정 요청: 최대 1회
- 단계 수정 요청: 단계당 최대 1회
- API 호출 타임아웃: 30초
- 자동 재시도: 없음
- 사용자의 명시적 재시도: 실패 단계당 1회

---

## 9. 기술 구성

4시간 MVP에 필요한 최소 구성만 사용한다.

```text
Browser
  ├─ React 또는 Next.js UI
  ├─ localStorage
  └─ HTTPS
       ▼
Azure Container Apps 또는 Azure Functions
  ├─ 단일 API
  ├─ Microsoft Agent Framework 순차 워크플로
  └─ GitHub Copilot SDK
```

### 구현 원칙

- 프론트엔드 상태는 브라우저 `localStorage`에 저장한다.
- 서버는 사용자 데이터를 영구 저장하지 않는다.
- AI 호출 자격 증명은 서버 환경 변수 또는 Azure Key Vault에만 저장한다.
- AI 응답은 서버에서 스키마 검증 후 클라이언트에 전달한다.
- API는 요청 단위로 실행하며 장기 작업 큐를 사용하지 않는다.
- 로그에는 목표와 결과물 원문 대신 오류 코드, 지연 시간, 요청 ID만 남긴다.

---

## 10. 데이터 및 안전 정책

- 사용 데이터는 기본적으로 현재 브라우저에만 저장한다.
- 사용자는 `새 목표 시작`으로 로컬 데이터를 즉시 삭제할 수 있다.
- 서버는 목표나 생성 결과물을 데이터베이스에 저장하지 않는다.
- 입력과 출력은 모델 학습에 사용하지 않는다는 안내를 표시한다.
- HTML을 생성 결과로 직접 실행하지 않고 일반 텍스트 또는 안전하게 렌더링한 Markdown만 표시한다.
- 시스템 프롬프트 변경, 비밀정보 요청, 외부 명령 실행은 허용하지 않는다.
- 외부 쓰기 도구가 없으므로 승인 게이트는 MVP에서 구현하지 않는다.

---

## 11. 비기능 요구사항

| 항목 | MVP 목표 |
|---|---|
| 초기 로딩 | 일반 네트워크에서 3초 이하 |
| 첫 상태 표시 | 요청 후 2초 이내 로딩 상태 표시 |
| AI 응답 | 각 요청 30초 이내, 초과 시 실패 처리 |
| 상태 복구 | 새로고침 후 2초 이내 로컬 상태 복원 |
| 모바일 | 360px 이상 지원 |
| 접근성 | 키보드 조작, 폼 라벨, 상태 텍스트 제공 |
| 브라우저 | 최신 Chrome과 Edge 우선 지원 |

---

## 12. 성공 지표

### 북극성 지표

**계획을 생성한 개인 사용자 중 완료 검토 화면까지 도달한 비율**

### MVP 측정 항목

- 목표 입력부터 계획 생성까지 걸린 시간
- 계획 승인 후 완료 검토까지 도달한 비율
- 완료 처리된 단계 수
- AI 수정 요청 횟수
- 사용자가 결과물을 복사하거나 내려받은 비율

해커톤 데모에서는 실제 사용자 분석 시스템 대신 현재 세션의 단계 수, 완료 수, 수정 횟수, 총 소요 시간을 완료 화면에 표시한다.

---

## 13. 데모 시나리오

### 목표

> “내일 5분 해커톤 발표를 준비하고 발표 대본과 점검표를 완성하고 싶어.”

### 완료 조건

1. 발표 흐름이 문제, 해결책, 데모, 효과 순서로 구성된다.
2. 5분 분량의 발표 대본이 있다.
3. 발표 직전 확인할 체크리스트가 있다.

### 시연 흐름

1. 사용자가 로그인 없이 목표와 완료 조건을 입력한다.
2. Planner가 발표 준비를 3~5개 단계로 분해한다.
3. 사용자가 계획을 승인한다.
4. Coach가 발표 구조, 대본 초안, 체크리스트를 순차 생성한다.
5. 사용자가 대본 단계에서 한 번 수정 요청한다.
6. Reviewer가 완료 조건별 상태와 근거를 보여준다.
7. 사용자가 전체 결과를 Markdown으로 내려받는다.
8. 새로고침 후 완료 상태가 유지되는 것을 보여준다.

---

## 14. 4시간 구현 계획

| 시간 | 구현 범위 |
|---|---|
| 0:00~0:30 | 프로젝트 생성, 단일 페이지 레이아웃, 타입 정의 |
| 0:30~1:20 | 목표 입력, 로컬 상태 저장, 화면 상태 전환 |
| 1:20~2:20 | Copilot SDK와 Agent Framework 연결, 계획·실행·검토 구조화 출력 |
| 2:20~3:10 | 단계 실행 UI, 수정·완료·건너뛰기, 오류·재시도 |
| 3:10~3:40 | 완료 검토, Markdown 내보내기, 반응형 UI |
| 3:40~4:00 | Azure 배포, 데모 데이터와 핵심 흐름 점검 |

### 시간 초과 시 축소 순서

1. 스트리밍을 일반 요청·응답으로 대체한다.
2. Microsoft Agent Framework 역할을 병렬 에이전트가 아닌 순차 함수로 구성한다.
3. Markdown 파일 내려받기를 복사 기능으로 대체한다.
4. 단계 수정 요청을 제외하고 완료·건너뛰기만 제공한다.

목표 입력, 계획 생성, 단계 결과물, 완료 검토의 핵심 흐름은 축소하지 않는다.

---

## 15. 주요 리스크와 완화

| 리스크 | 완화 |
|---|---|
| SDK 연동 지연 | 첫 30분 안에 최소 AI 호출을 확인하고 실패 시 단일 서버 API로 단순화 |
| 구조화 출력 실패 | JSON 스키마 검증과 한 번의 형식 교정 요청 사용 |
| AI 응답 지연 | 즉시 로딩 상태 표시, 30초 타임아웃, 명시적 재시도 |
| 데모 결과 변동 | 고정된 발표 준비 예시와 명확한 완료 조건 사용 |
| Azure 배포 지연 | 로컬 완성 후 단일 서비스만 배포하고 추가 Azure 서비스는 제외 |
| 범위 확장 | P0 외 기능은 구현하지 않고 데모 종료 후 백로그로 이동 |

---

## 16. 출시 체크리스트

- 목표와 완료 조건을 입력할 수 있다.
- AI 계획이 3~5개 단계로 표시된다.
- 계획 승인 후 단계별 결과물이 생성된다.
- 완료, 수정 요청, 건너뛰기가 상태에 반영된다.
- 완료 조건별 검토 결과가 표시된다.
- 전체 결과를 Markdown으로 복사하거나 내려받을 수 있다.
- 새로고침 후 상태가 복구된다.
- API 실패 시 오류와 재시도 버튼이 표시된다.
- 모바일 360px에서 핵심 버튼을 사용할 수 있다.
- Azure 배포 URL에서 전체 데모가 동작한다.
