# PRD — OnMyWay Agent Operations Harness

> **문서 상태:** 2026-08-22 최신 `main` 구현 기준
> **제품 형태:** 로그인 없는 반응형 웹 애플리케이션
> **핵심 기술:** GitHub Copilot SDK, Microsoft Agent Framework, Microsoft Azure

## 1. 제품 정의

OnMyWay는 여러 AI 에이전트 작업을 동시에 실행할 때 사용자가 각 작업의 목적,
현재 상태, 결과, 실패 원인을 파악하기 어려운 문제를 해결하는 에이전트 운영
하네스다.

Microsoft Agent Framework와 GitHub Copilot SDK로 작업을 실행하고, 각 작업을
`미실행 → 진행중 → 완료/실패` 상태로 시각화한다. 사용자는 여러 CLI 창이나
애플리케이션을 오가며 상태를 확인하는 대신 하나의 보드에서 작업을 작성, 실행,
추적, 재실행할 수 있다.

### 한 줄 가치 제안

> 흩어진 AI 작업의 실행 상태와 결과를 한 화면에 모아, 지금 무엇이 진행 중이고
> 어디에 대응해야 하는지 즉시 알 수 있게 한다.

## 2. 해결하려는 문제

GitHub Copilot CLI나 여러 AI 애플리케이션에서 작업을 병렬로 수행하면 다음 문제가
발생한다.

- 어떤 작업을 요청했는지 여러 창을 다시 열어 확인해야 한다.
- 작업별 대기, 실행, 완료, 실패 상태를 한눈에 비교하기 어렵다.
- 완료된 결과와 실패 원인이 서로 다른 세션에 흩어진다.
- 실패한 작업을 찾아 수정하고 다시 실행하는 과정에서 컨텍스트 전환이 발생한다.
- 동시에 처리하는 작업이 늘어날수록 실행 누락과 중복 실행 위험이 커진다.

OnMyWay는 AI 작업을 하나의 운영 단위로 저장하고 상태별 보드에 배치하여 이 문제를
해결한다.

## 3. 목표 사용자

### 주 사용자

- GitHub Copilot CLI나 AI 코딩 도구로 여러 작업을 동시에 처리하는 개발자
- 반복 프롬프트를 작성하고 결과를 추적하는 개인 사용자
- 여러 AI 작업의 진행 상황을 한 화면에서 확인하려는 사용자

### 핵심 Job-to-be-Done

> 여러 AI 작업을 실행할 때 각 작업의 목적, 현재 상태, 결과와 실패 원인을 한
> 화면에서 확인하고 필요한 작업만 빠르게 다시 실행하고 싶다.

### 사용 범위

- 로그인 없이 하나의 공유 워크스페이스로 동작한다.
- 프롬프트 한 건을 하나의 독립적인 에이전트 작업으로 취급한다.
- 여러 작업을 저장하고 실행 상태별로 조회할 수 있다.
- 팀 권한, 사용자별 데이터 분리와 외부 시스템 연동은 현재 범위에 포함하지 않는다.

## 4. 제품 목표

1. 사용자가 AI 작업을 제목, 프롬프트, 출력 형식과 함께 저장한다.
2. 실행 전 작업을 수정하거나 삭제한다.
3. 명시적인 확인 후 Microsoft Agent Framework 기반 하네스에서 작업을 실행한다.
4. 여러 작업의 진행 상태를 하나의 보드에서 확인한다.
5. 완료 결과와 실패 원인을 작업과 함께 보관한다.
6. 실패한 작업을 수정하거나 다시 실행한다.
7. 서버가 재시작되더라도 중단된 작업을 식별하고 안전하게 복구한다.

## 5. 핵심 사용자 여정

1. 사용자가 보드에서 새 프롬프트를 작성한다.
2. 미실행 열에서 작업 내용과 출력 형식을 검토한다.
3. 실행 버튼 또는 진행중 열로의 드래그를 통해 실행 확인 창을 연다.
4. 사용자가 실행을 승인하면 작업이 진행중 상태로 이동한다.
5. 백엔드가 Microsoft Agent Framework의 `GitHubCopilotAgent`를 통해 요청을 실행한다.
6. 화면이 상태를 갱신하고 작업을 완료 또는 실패 열에 표시한다.
7. 사용자는 결과 또는 실패 원인을 확인하고 실패 작업을 재실행한다.

## 6. 기능 요구사항

| 우선순위 | 기능 | 완료 조건 |
|---|---|---|
| P0 | 프롬프트 등록 | 제목과 요청 내용을 검증하고 미실행 작업으로 저장 |
| P0 | 실행 전 관리 | 미실행 또는 실패 작업을 수정·삭제 가능 |
| P0 | 명시적 실행 승인 | 확인 대화상자를 거친 뒤에만 실행 요청 |
| P0 | 에이전트 실행 | Agent Framework와 Copilot SDK를 통해 프롬프트 처리 |
| P0 | 다단계 검증 | Planner가 계획하고 Executor가 실행한 뒤 Reviewer가 결과를 검증하며, 수정 필요 시 1회 재실행 |
| P0 | 실시간 진행 표시 | 현재 에이전트 단계와 Executor 응답을 스트리밍으로 표시 |
| P0 | 상태 추적 | 미실행·진행중·완료·실패 상태와 개수를 보드에 표시 |
| P0 | 결과 보관 | AI 출력과 완료 시각을 작업에 저장 |
| P0 | 실패 처리 | 실패 원인을 표시하고 재실행 제공 |
| P0 | 실행 취소 | 진행중 작업을 서버에서 취소하고 실패 사유를 저장 |
| P0 | 동시 실행 보호 | 동일 작업에 대한 중복 실행 요청을 하나만 허용 |
| P0 | 중단 복구 | 서버 재시작 시 진행중 작업을 실패 상태로 전환 |
| P0 | 페이지 조회 | 상태별 작업이 많아져도 추가 페이지 조회 지원 |
| P0 | 반응형 UI | 데스크톱 4열, 태블릿 2열, 모바일 1열로 보드를 재배치하고 가로 스크롤 없이 핵심 기능 사용 가능 |
| P0 | Azure 배포 | 배포 환경에서 프론트엔드와 백엔드 상태 확인 성공 |

### 현재 비목표

- 사용자 계정, 조직, 역할 기반 권한
- 작업 간 의존성 및 자동 순차 실행
- 파일, 셸, URL 등 외부 도구 실행
- 장기 실행을 위한 분산 작업 큐
- 에이전트별 비용 및 토큰 대시보드
- 여러 장치 간 사용자별 동기화

## 7. 생산성 효과와 성공 지표

### 기대 효과

- 여러 CLI 창을 탐색하지 않고 전체 작업 상태를 확인한다.
- 완료 결과와 실패 원인을 찾는 시간을 줄인다.
- 중복 실행과 확인 누락을 방지한다.
- 실패 작업을 기존 입력과 함께 즉시 재실행한다.

### 측정 지표

| 지표 | 목표 |
|---|---:|
| 전체 작업 상태 파악 시간 | 10초 이내 |
| 특정 완료 결과 탐색 시간 | 15초 이내 |
| 상태 확인을 위한 CLI·앱 전환 횟수 | 기존 방식 대비 50% 이상 감소 |
| 완료·실패 작업 확인 누락 | 데모 시나리오에서 0건 |
| 실패 작업 재실행 | 원인 확인 후 1회 조작 |
| 첫 프롬프트 등록 | 설명 없이 2분 이내 |

해커톤 데모에서는 동일한 작업 세트를 CLI별로 확인하는 방식과 OnMyWay 보드에서
확인하는 방식을 비교해 상태 파악 시간과 화면 전환 횟수를 측정한다.

## 8. UX 원칙

- 상태는 색상뿐 아니라 텍스트로도 표현한다.
- 실행 전에 프롬프트와 출력 형식을 다시 보여주고 확인을 받는다.
- 진행중 작업은 자동 갱신하고 현재 처리 중임을 명확히 표시한다.
- 오류는 사용자 행동과 가까운 위치에 원인과 재시도 방법을 제공한다.
- 드래그를 사용할 수 없는 사용자를 위해 실행 버튼을 함께 제공한다.
- 작업 수가 많아져도 상태별 개수와 페이지 조회로 탐색할 수 있게 한다.
- 터치 환경에서는 수정·삭제 버튼을 항상 노출하고 작성·실행 확인 화면을 뷰포트
  안에서 스크롤할 수 있게 한다.

## 9. 책임 있는 AI와 데이터 정책

- 화면에서 결과가 Copilot이 생성한 응답임을 명확히 표시한다.
- 실행 전 사용자 확인을 요구한다.
- 클라이언트가 AI 모델이나 서버 자격 증명을 선택·전달하지 못하게 한다.
- 토큰과 데이터베이스 비밀번호는 서버 환경 변수로 관리한다.
- 에이전트에는 파일, 셸, URL 등 외부 도구 권한을 제공하지 않는다.
- 현재 버전은 프롬프트와 결과를 서버 데이터베이스에 저장한다는 사실을 사용자에게
  명확히 안내한다.

## 10. 데모 시나리오

1. 서로 다른 목적의 프롬프트 세 건을 등록한다.
2. 두 건을 실행해 여러 작업이 동시에 진행되는 모습을 보여준다.
3. 보드에서 미실행, 진행중, 완료, 실패 작업 수를 즉시 확인한다.
4. 완료 작업의 Copilot 결과를 확인한다.
5. 실패 작업의 원인을 확인하고 프롬프트를 수정해 재실행한다.
6. 페이지를 새로고침해 작업과 결과가 유지되는 것을 보여준다.
7. Azure 배포 환경에서 동일한 흐름과 상태 확인 API를 시연한다.

## 11. 심사 기준 연결

| 심사 항목 | 제품 근거 |
|---|---|
| Copilot SDK 및 Agent Framework | 공식 `GitHubCopilotAgent`를 실행 하네스의 핵심으로 사용 |
| 생산성 및 문제 적합성 | 분산된 AI 작업의 상태·결과 확인 비용과 누락 문제 해결 |
| Azure 통합 | 컨테이너 이미지 빌드와 Azure VM 배포 자동화 |
| 기능 완성도 | CRUD, 상태 전이, 중복 실행 방지, 중단 복구, 페이지 조회 |
| UX 및 워크플로 | 상태별 보드, 실행 확인, 자동 갱신, 오류 및 재실행 |
| 책임 있는 AI | AI 결과 표시, 실행 확인, 서버 측 모델·자격 증명 관리 |
| 혁신성 | 채팅 중심 AI 경험을 여러 에이전트 작업의 운영 화면으로 전환 |

## 12. 출시 체크리스트

- 프롬프트 작성, 수정, 삭제가 동작한다.
- 실행 전에 사용자 확인을 받는다.
- Copilot 응답이 완료 작업에 저장된다.
- 여러 작업의 상태와 개수가 정확하게 표시된다.
- 동일 작업의 중복 실행이 차단된다.
- 실패 원인을 확인하고 재실행할 수 있다.
- 서버 재시작으로 중단된 작업이 복구된다.
- 데스크톱·태블릿·모바일에서 보드가 각각 4열·2열·1열로 표시된다.
- 모바일에서 가로 스크롤 없이 작성, 수정, 삭제, 실행, 결과 확인이 가능하다.
- 전체 빌드와 테스트가 재현 가능하게 통과한다.
- Azure 배포와 상태 확인이 성공한다.
